.wrapper
  .container
    .content
      - if @posts.any?
        .posts
          = render :partial => 'post', :collection => @posts
        = paginate @posts
  = render :partial => 'layouts/sidebar'