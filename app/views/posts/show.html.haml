= form_for @res, url: post_path(@post) do |f|
  = f.text_area :content
  = f.submit "res", disable_with: 'Responsing...'
= render :partial => 'post', :locals => { :post => @post }
- if @ress.any?
  = render :partial => 'res', :collection => @ress